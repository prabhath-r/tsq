# The Second Question

## Start

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e .

tsq init
tsq study \
  --learner me \
  --topic c_ai_learning_systems \
  --mode diagnose \
  --limit 10 \
  --explain-policy
```
